put:
  $ref: "./other_deductions_create_and_amend.yaml"

  security:
    - User-Restricted:
        - write:self-assessment

get:
  $ref: "./other_deductions_retrieve.yaml"

  security:
    - User-Restricted:
        - read:self-assessment

delete:
  $ref: "./other_deductions_delete.yaml"

  security:
    - User-Restricted:
        - write:self-assessment
